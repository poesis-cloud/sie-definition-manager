{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "sie://schemas/gsm/v1/Interaction.schema.json",
    "title": "Interaction",
    "description": "Base ascription schema for Interaction Ascriptions. Defines the causal binding by referencing an Effector and a Receptor. Tenant-defined archetypes extending Interaction MUST reference this schema via allOf.",
    "$gsm:sealed": true,
    "$gsm:gsmClass": "Interaction",
    "type": "object",
    "required": [
        "effector",
        "receptor"
    ],
    "properties": {
        "effector": {
            "type": "string",
            "minLength": 1,
            "description": "ID reference to the Effector that is the emitting endpoint."
        },
        "receptor": {
            "type": "string",
            "minLength": 1,
            "description": "ID reference to the Receptor that is the receiving endpoint."
        }
    }
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sie://schemas/gsm/v1/Realization.json",
  "title": "Realization (GSM v1)",
  "description": "Traced value delivery path linking a Function to a Purpose through the Rule that enables delivery.",
  "allOf": [
    { "$ref": "sie://schemas/gsm/v1/SystemicPrimitive.json" }
  ],
  "type": "object",
  "properties": {
    "rule": {
      "anyOf": [
        { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
        { "type": "null" }
      ],
      "description": "Rule through which the Function→Purpose delivery is enabled (null = ungrounded)."
    },
    "function": {
      "anyOf": [
        { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
        { "type": "null" }
      ],
      "description": "Function being realized (null = purpose without capability)."
    },
    "purpose": {
      "anyOf": [
        { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
        { "type": "null" }
      ],
      "description": "Purpose being served (null = capability without purpose)."
    }
  },
  "allOf": [
    {
      "anyOf": [
        { "required": ["rule", "function"] },
        { "required": ["rule", "purpose"] },
        { "required": ["function", "purpose"] }
      ]
    }
  ]
}

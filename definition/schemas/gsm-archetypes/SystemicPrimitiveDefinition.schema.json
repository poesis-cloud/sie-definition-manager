{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sie://schemas/gsm/v1/SystemicPrimitiveDefinition.json",
  "title": "SystemicPrimitiveDefinition (GSM v1)",
  "description": "Base type for all systemic primitives. Extension should be done via allOf in downstream adapters (e.g., ITIP). Domain-specific property values are governed through the Qualifier primitive (Organization-level, DPQ chain).",
  "type": "object",
  "required": ["id", "definitionStatus"],
  "properties": {
    "id": { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
    "definitionStatus": {
      "type": "string",
      "enum": ["DRAFT", "PROPOSED", "APPROVED", "ACTIVE", "SUSPENDED", "DEPRECATED", "RETIRED", "REJECTED"],
      "description": "Unified lifecycle status for all systemic primitive instances."
    }
  }
}

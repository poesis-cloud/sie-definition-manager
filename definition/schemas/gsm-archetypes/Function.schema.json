{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sie://schemas/gsm/v1/Function.json",
  "title": "Function (GSM v1)",
  "description": "An activity or process that the system can perform — the name of what a Mechanism does. Each Mechanism references exactly one Function. Function↔Purpose is derivable through the structural chain: Function ← Mechanism/Controller → Rule → Operationalization → Policy → Directive → Purpose.",
  "allOf": [
    {
      "$ref": "sie://schemas/gsm/v1/SystemicPrimitiveDefinition.json"
    }
  ],
  "type": "object",
  "required": [
    "name"
  ],
  "properties": {
    "name": {
      "$ref": "sie://schemas/gsm/v1/common.json#/definitions/Name80"
    }
  }
}
{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "sie://schemas/gsm/v1/Effector.schema.json",
    "title": "Effector",
    "description": "Base ascription schema for Effector Ascriptions. An Effector is an output port of a Mechanism, derived by the definition-manager from the Mechanism rule's AST. It references the owning Mechanism and the Archetype that types the emitted data. Tenant-defined archetypes MUST NOT extend Effector.",
    "$gsm:sealed": true,
    "$gsm:gsmClass": "Effector",
    "$gsm:internal": true,
    "type": "object",
    "required": [
        "mechanism",
        "archetype"
    ],
    "properties": {
        "mechanism": {
            "type": "string",
            "minLength": 1,
            "description": "ID reference to the owning Mechanism."
        },
        "archetype": {
            "type": "string",
            "minLength": 1,
            "description": "ID reference to the Archetype that types the Effector's output payload."
        },
        "interface": {
            "type": [
                "string",
                "null"
            ],
            "minLength": 1,
            "description": "Optional ID reference to the Interface that exposes this Effector for inter-Structure coupling. Null means the port is private to the Structure."
        }
    },
    "additionalProperties": false
}

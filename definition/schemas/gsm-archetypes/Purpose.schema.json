{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sie://schemas/gsm/v1/Purpose.json",
  "title": "Purpose (GSM v1)",
  "description": "A reason for the system's existence — an intended outcome or objective. Function↔Purpose is derived through Realization, not stored.",
  "allOf": [
    { "$ref": "sie://schemas/gsm/v1/SystemicPrimitive.json" }
  ],
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/Name80" }
  }
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sie://schemas/gsm/v1/Controller.json",
  "title": "Controller (GSM v1)",
  "description": "Mechanism that implements Rules and executes their Control sequences. Auto-defined from the Rules it implements.",
  "allOf": [
    { "$ref": "sie://schemas/gsm/v1/Mechanism.json" }
  ],
  "type": "object",
  "properties": {
    "functions": {
      "type": "array",
      "items": { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
      "description": "References to Function primitives realized by this controller."
    },
    "rules": {
      "type": "array",
      "items": { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
      "description": "References to Rule primitives implemented by this controller."
    }
  }
}

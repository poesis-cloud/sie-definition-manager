{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sie://schemas/gsm/v1/Directive.schema.json",
  "title": "Directive",
  "description": "Governance DNA archetype for Directive — identity-level normative constraints (DNA 'D'). Standalone first-class GSM class with independent lifecycle, typed by DirectiveArchetype. Fields: structure, purpose, modal, verb, qualifier.",
  "$gsm:sealed": true,
  "$gsm:gsmClass": "Directive",
  "type": "object",
  "required": [
    "structure",
    "modal",
    "verb",
    "qualifier",
    "purpose"
  ],
  "examples": [
    {
      "structure": "019cc49a-dc59-7fef-9b0c-b3f100044603",
      "modal": "MUST",
      "verb": "ENSURE",
      "qualifier": "019cc49a-dc5a-7670-9ccb-5e161985340b",
      "purpose": "019cc49a-dc5a-7003-98e1-c214032f0420"
    }
  ],
  "properties": {
    "structure": {
      "type": "string",
      "minLength": 1,
      "description": "ID reference to the authoring Structure that issues the Directive."
    },
    "modal": {
      "type": "string",
      "enum": [
        "MUST",
        "MUST_NOT",
        "SHOULD",
        "SHOULD_NOT",
        "MAY"
      ],
      "description": "Deontic obligation strength. MUST = obligatory (violation = non-compliance). MUST_NOT = forbidden (violation = non-compliance). SHOULD = recommended (violation = warning). SHOULD_NOT = discouraged (violation = warning). MAY = permitted (informational)."
    },
    "verb": {
      "type": "string",
      "enum": [
        "ENSURE",
        "PREVENT",
        "MAINTAIN",
        "OPTIMIZE",
        "MINIMIZE",
        "MAXIMIZE",
        "MONITOR",
        "ENABLE"
      ],
      "description": "Governance action expressing what the governance intent does to the quality dimension."
    },
    "qualifier": {
      "type": "string",
      "minLength": 1,
      "description": "ID reference to the Archetype that defines the viability dimension being governed. The Archetype's schema defines the measurable properties; Norms operationalizing this Directive MUST reference this same Archetype in their guard/predicate CEL expressions."
    },
    "purpose": {
      "type": "string",
      "minLength": 1,
      "description": "ID reference to the purposed Structure."
    }
  },
  "additionalProperties": false
}

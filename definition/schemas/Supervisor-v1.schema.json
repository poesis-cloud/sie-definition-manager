{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sie://schemas/gsm/v1/Supervisor.json",
  "title": "Supervisor (GSM v1)",
  "description": "Mechanism enforcing policies, rules, and controls in real time.",
  "allOf": [
    { "$ref": "sie://schemas/gsm/v1/Mechanism.json" }
  ],
  "type": "object",
  "anyOf": [
    { "required": ["enforcingPolicies"] },
    { "required": ["enforcingRules"] },
    { "required": ["enforcingControls"] }
  ],
  "properties": {
    "enforcingPolicies": {
      "type": "array",
      "items": { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
      "description": "References to Policy primitives enforced by this supervisor."
    },
    "enforcingRules": {
      "type": "array",
      "items": { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
      "description": "References to Rule primitives enforced by this supervisor."
    },
    "enforcingControls": {
      "type": "array",
      "items": { "$ref": "sie://schemas/gsm/v1/common.json#/definitions/UUID" },
      "description": "References to Control primitives enforced by this supervisor."
    }
  }
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sie://schemas/events/v1/ActuationFeedbackEvent.json",
  "title": "ActuationFeedbackEvent (Events v1)",
  "description": "Feedback emitted from actuation execution.",
  "type": "object",
  "additionalProperties": false,
  "required": ["operation", "objectArchetypeName"],
  "properties": {
    "operation": {
      "type": "string",
      "enum": ["create", "modify", "delete", "acquire"]
    },
    "objectArchetypeName": { "type": "string" },
    "criteria": {
      "description": "LogicalOperatorNode (nullable).",
      "type": ["object", "null"],
      "additionalProperties": true
    },
    "valueMapping": {
      "description": "Map (nullable).",
      "type": ["object", "null"],
      "additionalProperties": true
    }
  }
}
